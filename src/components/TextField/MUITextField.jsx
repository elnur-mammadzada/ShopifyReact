import { TextField } from '@mui/material'
import React from 'react'

const MUITextField = ({ className }) => {
    return (

        <TextField className={className} />

    )
}

export default MUITextField
